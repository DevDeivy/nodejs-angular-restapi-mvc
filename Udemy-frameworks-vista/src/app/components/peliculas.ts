export class Peliculas {
    // title?: string;
    // year?: number;
    // descripcion?: string;
    // img?: string;

    // constructor(title: string, year: number, img: string, descripcion: string) {
    //     this.title = title;
    //     this.year = year;
    //     this.descripcion = descripcion;
    //     this.img = img;
    // }

    //esta es una manera mas corta y mas legible. es el mismo codigo que el anterior
    constructor(
        public title: string, 
        public year: number, 
        public img: string, 
        public descripcion: string) {}}